# 📹 YouTube Converter - Backend

API encargada de procesar las solicitudes de descarga y conversión de videos de YouTube a formatos MP3/MP4.

## 🛠️ Tecnologías
* **Lenguaje:** [Java 17 / Node.js] *(Edita según corresponda)*
* **Framework:** [Spring Boot / Express]
* **Herramientas:** [Librería usada, ej: Youtube-dl / FFmpeg]

## 🚀 ¿Cómo ejecutarlo?

1.  **Clonar el repositorio:**
    ```bash
    git clone <URL_DEL_REPO>
    ```
2.  **Instalar dependencias:**
    ```bash
    # Si es Java (Maven)
    mvn clean install
    
    # Si es Node.js
    npm install
    ```
3.  **Ejecutar el servidor:**
    El servidor iniciará en `http://localhost:8080` (por defecto).
